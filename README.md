# AI-Powered Partner Data Validator (FastAPI)

Validates uploaded partner datasets using FastAPI + Pydantic schemas and returns structured error reporting.

## Run
pip install -r requirements.txt
uvicorn app.main:app --reload
